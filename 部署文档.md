# 智慧养老（zzyl）项目 Docker 部署文档

> 适用版本：后端 `zzyl-web`（Spring Boot + activiti 7.10），前端 `project-zhyl-admin-vue3-java`（Vue3 + Vite），小程序 `mp-weixin` / `project-zhyl-xcx-uniapp-java`（微信小程序）。
> 默认服务器 IP：`172.168.20.222`（以下所有地址均以此为准；若换机器请全局替换）。

---

## 一、部署架构概览

| 组件 | 技术栈 | 容器/部署方式 | 端口 | 说明 |
|---|---|---|---|---|
| MySQL | 5.7 | Docker | 3306 | 库名 `zhyl-auth`（含连字符，建库需反引号） |
| Redis | 6.x | Docker | 6379 | 密码 `123456`，与 `application.yml` 一致 |
| xxl-job-admin | 2.3.0 | Docker | 8889→8080 | 调度中心，地址 `http://172.168.20.222:8889/xxl-job-admin` |
| 后端 zzyl-web | Spring Boot | Jar / Docker | 9995 | 执行器端口 9999（自动注册） |
| 前端 admin | Vue3 + Nginx | Docker(Nginx) | 80 | 构建产物 `dist/` |
| 小程序 | 微信原生 / UniApp | 微信开发者工具上传 | — | 不走 Docker |

---

## 二、环境准备

1. 服务器安装 Docker 与 Docker Compose：
   ```bash
   # Docker
   curl -fsSL https://get.docker.com | sh
   systemctl enable --now docker
   # Compose（v2 插件）
   docker compose version
   ```
2. 开放防火墙 / 安全组端口：`3306`、`6379`、`8889`、`9995`、`80`（前端）、`9999`（执行器，仅内网）。
3. 后端构建机需 **JDK 11+**（activiti-engine 7.10.0 为 Java 11 字节码；切勿用 JDK 8 构建）。

---

## 三、基础中间件部署（Docker）

### 3.1 MySQL 5.7
```bash
docker run -d --name zzyl-mysql \
  -p 3306:3306 \
  -e MYSQL_ROOT_PASSWORD=123456 \
  -v /data/mysql:/var/lib/mysql \
  --restart=always \
  mysql:5.7 --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```
**建库注意（项目已知坑）**：库名 `zhyl-auth` 含连字符，MySQL 会把 `-` 当成减号，直接在 `CREATE DATABASE` / `USE` 里写会报 `1064`。必须用反引号包裹：
```sql
CREATE DATABASE IF NOT EXISTS `zhyl-auth` DEFAULT CHARACTER SET utf8mb4;
USE `zhyl-auth`;
```
> 导入 `zhyl-auth.sql` 前，请确认 SQL 内 `CREATE DATABASE`/`USE` 已用反引号；若历史备份未加反引号，需先手动修正再导入。

### 3.2 Redis
```bash
docker run -d --name zzyl-redis \
  -p 6379:6379 \
  -v /data/redis:/data \
  --restart=always \
  redis:6-alpine redis-server --requirepass 123456
```
> 密码 `123456` 需与 `application.yml` 的 `spring.redis.password` 一致。

### 3.3 xxl-job-admin
```bash
docker run -d --name zzyl-xxl-job-admin \
  -p 8889:8080 \
  -v /data/xxl-job:/data/applogs \
  --restart=always \
  xuxueli/xxl-job-admin:2.3.0
```
- 访问 `http://172.168.20.222:8889/xxl-job-admin`，默认账号 `admin/123456`。
- **新增执行器**：左侧「执行器管理 → 新增」，AppName 填 **`zzyl-dev-executor`**（必须与后端 `application.yml` 的 `xxl.job.executor.appname` 完全一致），注册方式选「自动注册」，机器地址留空。保存并重启后端后，执行器显示「Online 机器地址」。
- 现有任务处理器（JobHandler）：`tradingJob`、`reservationStatusToExpired`、`clearDeviceDataJob`、`createMothBillAndTaskJob`。

### 3.4 一键编排（可选 docker-compose.yml）
```yaml
version: "3"
services:
  mysql:
    image: mysql:5.7
    container_name: zzyl-mysql
    ports: ["3306:3306"]
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes: ["/data/mysql:/var/lib/mysql"]
    restart: always
  redis:
    image: redis:6-alpine
    container_name: zzyl-redis
    ports: ["6379:6379"]
    command: redis-server --requirepass 123456
    volumes: ["/data/redis:/data"]
    restart: always
  xxl-job-admin:
    image: xuxueli/xxl-job-admin:2.3.0
    container_name: zzyl-xxl-job-admin
    ports: ["8889:8080"]
    volumes: ["/data/xxl-job:/data/applogs"]
    restart: always
```
启动：`docker compose up -d`。

---

## 四、后端 zzyl-web 部署

### 4.1 本地打包
```bash
# 必须使用 JDK 11+
export JAVA_HOME=/path/to/jdk-17
mvn -version
cd d:/MyCode/zzyl
mvn clean package -pl zzyl-web -am -DskipTests
# 产物：zzyl-web/target/zzyl-web-*.jar
```

### 4.2 准备配置文件
`application.yml` 已被 `.gitignore` 排除（含密钥，禁止提交）。部署前复制模板并填写：
```bash
cp zzyl-web/src/main/resources/application-example.yml \
   zzyl-web/src/main/resources/application.yml
```
关键项（**必须指向本机服务器 `172.168.20.222`**）：
```yaml
spring:
  datasource:
    url: jdbc:mysql://172.168.20.222:3306/zhyl-auth?...
    username: root
    password: 123456
  redis:
    host: 172.168.20.222
    port: 6379
    password: 123456
xxl:
  job:
    admin:
      addresses: http://172.168.20.222:8889/xxl-job-admin   # 切勿写教室地址 192.168.200.146
    executor:
      appname: zzyl-dev-executor
      port: 9999
framework:
  ali:
    oss:
      endpoint: oss-cn-xxxx.aliyuncs.com
      accessKeyId: ${ALIYUN_OSS_AK}      # 换成你自己的阿里云 AK
      accessKeySecret: ${ALIYUN_OSS_SK}  # 切勿使用黑马教学失效 key
      bucketName: 你自己的bucket
```
> **OSS 说明**：原配置使用的是黑马教学公共密钥（`LTAI5tDQKg9F61aJhbmhqVRK`），已被阿里云回收，启动会报 `InvalidAccessKeyId`。代码已改为懒加载 + 异常兜底，因此**启动不再崩溃**，但文件上传功能需换成你自己的 AK/SK 与自建 bucket 后才可用。

### 4.3 后端 Dockerfile（仓库暂无，需新增）
在 `zzyl-web/` 下创建 `Dockerfile`：
```dockerfile
FROM openjdk:11-jre-slim
WORKDIR /app
COPY target/zzyl-web-1.0-SNAPSHOT.jar app.jar
EXPOSE 9995
ENTRYPOINT ["java","-jar","app.jar","--spring.profiles.active=prod"]
```
> 将 `zzyl-web-1.0-SNAPSHOT.jar` 替换为实际构建出的 jar 名。

### 4.4 构建运行
```bash
cd zzyl-web
docker build -t zzyl-web .
docker run -d --name zzyl-web \
  -p 9995:9995 \
  -e ALIYUN_OSS_AK=你的AK -e ALIYUN_OSS_SK=你的SK \
  --restart=always zzyl-web
```
或直接以 Jar 运行（服务器已装 JDK）：`java -jar zzyl-web-*.jar`。

---

## 五、前端 admin 部署

### 5.1 构建
```bash
cd zzyl-ui/project-zhyl-admin-vue3-java
# 设置后端地址（VITE_BASE_URL 指向网关/后端）
export VITE_BASE_URL=http://172.168.20.222:9995
npm install
npm run build      # 产物输出到 dist/
```
> 注意：`vue-tsc` 类型检查较慢，CI 可加 `--mode` 或跳过；`vite.config.ts` 的 mock（`prodEnabled`）生产必须关闭，否则 mock 打进生产包。

### 5.2 容器化（已含 Dockerfile）
前端仓库已提供 `Dockerfile`（基于 nginx，拷贝 `dist/` 与 `default.conf`）：
```bash
docker build -t zzyl-admin .
docker run -d --name zzyl-admin -p 80:80 --restart=always zzyl-admin
```
需自行准备 `default.conf`（反向代理后端 `/api` 到 `172.168.20.222:9995`）：
```nginx
server {
  listen 80;
  location / { root /usr/share/nginx/html; index index.html; try_files $uri $uri/ /index.html; }
  location /api/ { proxy_pass http://172.168.20.222:9995/; }
}
```

---

## 六、小程序部署（不走 Docker）

- **`mp-weixin`**：微信开发者工具「导入项目」→ 选择该目录 → 在「详情 → 本地设置」填入**真实 request 合法域名**（如 `https://api.zzyl.com`），并开启 `urlCheck`（勿用 `localhost`）。上传代码后提交审核发布。
- **`project-zhyl-xcx-uniapp-java`**：HBuilderX 打开 → 发行 → 微信小程序，编译产物即 `mp-weixin`。注意 `pages.json` 中勿把 `Foot/uni-phone` 等组件误登记为页面；`utils/env.js` 的接口域名按环境切换，勿硬编码 `localhost`。

---

## 七、启动顺序与验证

1. 启动顺序：MySQL → Redis → xxl-job-admin → 后端 zzyl-web → 前端 admin。
2. 后端健康检查：`curl http://172.168.20.222:9995/` 或查看日志无 `APPLICATION FAILED TO START`。
3. xxl-job：控制台「执行器管理」中 `zzyl-dev-executor` 显示 Online；「任务管理」可手动「执行一次」验证 `tradingJob` 等任务。
4. 前端：浏览器访问 `http://172.168.20.222` 能打开登录页，接口联通。

---

## 八、常见问题（FAQ）

| 现象 | 原因 | 解决 |
|---|---|---|
| `OSSException: InvalidAccessKeyId` | 使用了黑马教学失效密钥 | 换成自己的 AK/SK + 自建 bucket（启动已不崩溃，仅上传受影响） |
| `xxl-job ... connect timed out` | `addresses` 写成教室地址 `192.168.200.146:8888` | 改为 `http://172.168.20.222:8889/xxl-job-admin` |
| `MySQL 1064` 导入报错 | `CREATE DATABASE/USE zhyl-auth` 未加反引号 | 用 `` `zhyl-auth` `` 包裹库名 |
| 后端连不上 Redis/MySQL | `application.yml` 仍指向教室 `172.168.20.222` 以外地址 | 统一改为 `172.168.20.222` |
| 执行器一直 Offline | AppName 与控制台不一致 / 端口 9999 被占用 | 核对 `appname`，换执行器端口 |
| 前端白屏/接口 404 | `VITE_BASE_URL` 或 nginx 反代未指向后端 | 检查 `default.conf` 的 `proxy_pass` |

---

## 九、安全与密钥管理建议

- `application.yml` 含数据库密码、OSS、微信 `appSecret`、JWT key 等，**禁止提交到远程仓库**（已 gitignore）。
- 生产建议用环境变量 / Nacos 注入密钥，避免明文：`ALIYUN_OSS_AK`、`ALIYUN_OSS_SK` 等。
- 若历史仓库曾 push 过含密钥的 `application.yml`，相关密钥已被暴露，**必须到对应控制台轮换**并用 `git filter-repo`/BFG 清理历史后强制推送。
- xxl-job-admin 默认密码 `admin/123456` 上线前应修改。
