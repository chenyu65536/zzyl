<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzyl.mapper.DishesTypeMapper">

    <insert id="insert" parameterType="com.zzyl.entity.DishesType" useGeneratedKeys="true" keyProperty="id">
        insert into dishes_type(name, del_flag, create_by, remark, create_time)
        values (#{name}, 0, #{createBy}, #{remark}, #{createTime})
    </insert>

    <update id="update" parameterType="com.zzyl.entity.DishesType">
        update dishes_type
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = #{updateTime}
        </set>
        where id = #{id}
    </update>

    <select id="findById" parameterType="java.lang.Long" resultType="com.zzyl.entity.DishesType">
        select *
        from dishes_type
        where id = #{id}
          and del_flag = 0
    </select>

    <select id="findByName" resultType="com.zzyl.entity.DishesType">
        select *
        from dishes_type
        where name = #{name}
          and del_flag = 0
    </select>

    <select id="findAll" resultType="com.zzyl.entity.DishesType">
        select *
        from dishes_type
        where del_flag = 0
        <if test="name != null and name != ''">
            and name like concat('%', #{name}, '%')
        </if>
        order by create_time desc
    </select>

    <select id="countAll" resultType="int">
        select count(*)
        from dishes_type
        where del_flag = 0
    </select>
</mapper>
