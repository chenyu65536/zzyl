#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/code/2023/project-zhyl-admin-vue3-java/node_modules/.store/conventional-commits-parser@4.0.0/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/code/2023/project-zhyl-admin-vue3-java/node_modules/.store/conventional-commits-parser@4.0.0/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../conventional-commits-parser@4.0.0/node_modules/conventional-commits-parser/cli.js" "$@"
else
  exec node  "$basedir/../../../../../../conventional-commits-parser@4.0.0/node_modules/conventional-commits-parser/cli.js" "$@"
fi
